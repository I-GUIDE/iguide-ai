# Web framework / server
flask
flask-cors
flasgger
gunicorn
fastapi

# HTTP clients
aiohttp
httpx
requests

# LLM / agent stack
openai
langchain>=1.0
langchain-core>=1.0
langchain-openai>=1.0
# Claude as an ANSWERING provider (the code peer talks to the CLI instead, and needs none
# of this). Selectable per request alongside OpenAI and AnvilGPT; inert without a key.
langchain-anthropic>=1.0
langgraph>=1.0
langchain-mcp-adapters
mcp[cli]>=1.2

# Search backends
opensearch-py
neo4j
ddgs

# Open-web page extraction (web_fetch): boilerplate removal + HTML -> markdown.
# Present in dev environments already; unpinned they are absent from the Docker image.
beautifulsoup4
lxml
markdownify

# Embeddings / ML
sentence-transformers
transformers
--extra-index-url https://download.pytorch.org/whl/cpu
torch
spacy

# Geospatial
geopandas
shapely
fiona
rasterio

# Spatial statistics (agent_runtime/analysis_spatial_stats_tools.py).
# libpysal = spatial weights, esda = Moran/Geary/LISA/Getis-Ord, spreg = spatial regression.
# pygeoda is GeoDa's own C++ core (libgeoda) and powers the regionalization tool; GeoDa
# itself is a desktop GUI with no scripting interface, so this binding is the scriptable path.
# All four ship wheels, so none of them needs a compiler in the image.
libpysal
esda
spreg
pygeoda

# Data / notebooks
pandas
matplotlib
nbformat

# Storage / cloud
boto3

# Config / testing
python-dotenv
pytest
pydantic

# Verifies the platform's HS256 access token in token mode (agent_runtime/identity.py).
# Pinned explicitly although it already arrives transitively: identity must not stop
# working because something else dropped its dependency on it.
PyJWT>=2.8
